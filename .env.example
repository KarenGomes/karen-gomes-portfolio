# Copie para `.env` na raiz e preencha. O Vite carrega `.env` e `.env.local`.
# Valores VITE_* são embutidos no bundle do cliente (visíveis no navegador).

VITE_API_URL=
VITE_WEB3FORMS_ACCESS_KEY=
VITE_LINKEDIN_PROFILE_URL=
VITE_HCAPTCHA_SITEKEY=
